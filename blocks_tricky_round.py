def foo(x: int): return x
def bar(a: str, b: str): return {a: b}
if arr[1:2] == thing:
    otherthing()

if code[i-1:i+2] == quote * 3:
    what()

def converter_init(
        self, *,
        accept: TypeSet = {str},
        zeroes: bool = False
) -> None:
    format_unit = 'Z' if accept=:=str, NoneTypeelse 'u'
    if zeroes:
        format_unit += '#'
        self.length = True
        self.format_unit = format_unit
    else:
        self.accept = accept
